<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- Compiled and minified CSS -->
    <style>
      body {
        background-color: gray;
        height: 1050px;
        width: 1920 px;
      }

      .brand-logo {
        margin-left: 20px;
      }
      .col .s12 {
        margin-left: 2px 2px 2px 20px;
        background-color: #546e7a;
        height: 1050px;
        box-shadow: 0px 2px 2px 2px #455a64;
      }

      .nav-wrapper {
        box-shadow: 2px 0px 2px 0px #90a4ae;
      }
    </style>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css"
    />

    <!-- Compiled and minified JavaScript -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
    <title><%= title %></title>
  </head>
  <body>
    <div class="row">
      <div class="col s3"></div>
      <div class="col s6">
        <div class="row">
          <nav>
            <div class="nav-wrapper blue-grey lighten-2">
              <a href="/chat" class="brand-logo">Back</a>
            </div>
          </nav>
          <div class="col s12">
            Account Owner : <%=owner.username%>
            <hr />
            <ul>
              <% names.forEach(name =>{ %>
              <li class="badges">
                <a href="/chat/<%=owner.username%>-<%=name%>" class="white-text"
                  ><%=name%>
                </a>
              </li>
              <% }) %>
            </ul>
          </div>
        </div>
      </div>
    </div>
    <script src="/socket.io/socket.io.js"></script>
    <script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
    <script src="js/socket.js"></script>
  </body>
</html>
